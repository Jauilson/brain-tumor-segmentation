# Brain Tumor Segmentation - Requirements
# Python 3.8+
#
# Two environments supported:
#   - Original notebook: tensorflow>=2.10, keras>=2.10
#   - Optimized v3:      tensorflow>=2.16, keras>=3.0 (standalone)
#
# Google Colab 2025 ships with TF 2.19 + Keras 3.13 — no install needed.
# For local use, install the appropriate version below.

# ── Deep Learning ──────────────────────────────────────────────────────────
# Original notebook (TF < 2.16):
#   tensorflow>=2.10.0,<2.16.0
#   keras>=2.10.0,<3.0.0
#
# Optimized v3 (TF 2.16+ with standalone Keras 3):
tensorflow>=2.16.0
keras>=3.0.0

# ── Medical Image Processing ───────────────────────────────────────────────
nibabel>=4.0.0          # NIfTI file loading (.nii.gz)
pydicom>=2.3.0          # DICOM support (optional)

# ── Numerical Computing ────────────────────────────────────────────────────
numpy>=1.23.0
scipy>=1.9.0            # zoom, ndimage operations

# ── Image Processing ───────────────────────────────────────────────────────
scikit-image>=0.19.0    # resize, transform
opencv-python>=4.6.0    # optional, for augmentation
Pillow>=9.2.0

# ── Data Science ───────────────────────────────────────────────────────────
pandas>=1.4.0
scikit-learn>=1.1.0     # train_test_split

# ── Visualization ──────────────────────────────────────────────────────────
matplotlib>=3.5.0
seaborn>=0.12.0

# ── Utilities ──────────────────────────────────────────────────────────────
tqdm>=4.64.0
h5py>=3.7.0             # .h5 model loading (legacy format)
psutil>=5.9.0           # RAM usage monitoring (optional)

# ── Jupyter ────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.15.0
ipywidgets>=8.0.0

# ── Development (optional) ─────────────────────────────────────────────────
# black>=22.6.0
# flake8>=5.0.0
# pytest>=7.1.0
